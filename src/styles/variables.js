/**
 * Default (global) variables for `postcss-simple-vars`.
 *
 * Used instead of custom properties where those cannot be applied:
 * - Media queries
 *
 * Since variables are not part of the standard, they are also mapped to custom
 * properties to reduce its usage to the minimum.
 */

export const variables = {
  'viewport-padding-x': '20px'
};
